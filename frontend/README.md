# Minimal Frontend

## Run

1. `cd frontend`
2. `npm install`
3. `npm run dev`

By default, API requests go to `http://localhost:5000`.

To change backend URL:

1. Copy `.env.example` to `.env`
2. Set `VITE_API_BASE_URL` to your backend URL
